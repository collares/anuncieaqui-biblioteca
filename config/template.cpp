#include <iostream>
#include <cstdio>
#include <algorithm>
#include <vector>
#include <set>
#include <queue>
#include <map>
#include <utility>
#include <cstring>
#include <cstdlib>
#include <cmath>

using namespace std;

typedef double TYPE;
const TYPE EPS = 1e-9;

inline int sgn(TYPE a) { return a > EPS ? 1 : (a < -EPS ? -1 : 0); }
inline int cmp(TYPE a, TYPE b) { return sgn(a - b); }

int main() {
    return 0;
}
